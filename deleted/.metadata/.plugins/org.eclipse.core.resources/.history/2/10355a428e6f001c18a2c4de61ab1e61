package chap05;

public class Sort extends Lottery {
	
	void selectionSort() {
		for (int i=0;i<lotto.length;i++) {
			for(int j=0;j<lotto.length;j++) {
				if(lotto[i]<lotto[j]) {
					int ch = lotto[i];
					lotto[i] = lotto[j];
					lotto[j] = ch;
				}
			}
		}

	}
}
